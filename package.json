{
  "name": "flower-shop-mock-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "json-server --watch db.json --port 3001",
    "start": "npm run dev & npm run server"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "json-server": "^0.17.4"
  }
}